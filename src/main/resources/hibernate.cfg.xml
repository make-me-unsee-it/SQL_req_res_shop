<hibernate-configuration>
    <session-factory>
        <!-- DATABASE CONNECTION SETTINGS -->
        <property name ="hibernate.connection.driver_class">org.h2.Driver</property>
        <property name ="hibernate.connection.username">sa</property>
        <property name ="hibernate.connection.password"/>
        <property name ="hibernate.connection.url">jdbc:h2:mem:online-shop</property>
        <property name ="hibernate.id.new_generator_mappings">false</property>

        <!-- SQL DIALECT -->
        <property name ="hibernate.dialect">org.hibernate.dialect.H2Dialect</property>

        <!-- ECHO ALL EXECUTED SQL TO STDOUT -->
        <property name ="show_sql">true</property>

        <!-- JDBC CONNECTION POOL -->
        <property name ="connection.pool_size">10</property>

        <!-- DROP AND RE-CREATE THE DATABASE SCHEMA ON STARTUP -->
        <property name ="hibernate.hbm2ddl.auto">create</property>

        <!-- PREPOPULATE DATABASE WITH THIS DML SCRIPT -->
        <property name ="hibernate.hbm2ddl.import_files">db.init/addIntoTables.sql</property>

        <!-- NAMES THE ANNOTATED ENTITY CLASS -->
        <mapping class="com.step.hryshkin.model.Good"/>
        <mapping class="com.step.hryshkin.model.OrderGood"/>
        <mapping class="com.step.hryshkin.model.Order"/>
        <mapping class="com.step.hryshkin.model.User"/>
    </session-factory>
</hibernate-configuration>